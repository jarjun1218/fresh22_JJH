<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <script src="https://cdn.tailwindcss.com"></script>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>
</head>
<body>
    <div class="container">
        <h1 class="mb-4">Edit Message</h1>

        <form action="/articles/<%= article.id %>?_method=put" method="post">
            <div class="form-group">
                <label for="title">Title</label>
                <input required value="<%= article.title %>" type="text" name="title" id="title" class="form-control">    
            </div>
            <div class="form-group">
                <label for="innertext">Inner Text</label>
                <textarea required name="innertext" id="innertext" class="form-control"><%= article.innertext %></textarea>
            </div>
            
            <button type="submit" class="btn btn-primary">Save</button>
            <a href="/articles/<%= article.id %>" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
    
</body>
</html>